[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = [
    'npm ci',
    'cd client && npm ci',
    'cd server && npm ci'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'cd server && npm start' 